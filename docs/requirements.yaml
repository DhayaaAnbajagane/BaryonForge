name: pyccl_env

channels:
  - conda-forge
  - anaconda
  - defaults

dependencies:
  - pyccl>=3.1.2
  - numpy==1.*
  - scipy>=1.12
  - joblib
  - tqdm
  - healpy

  - pip  

  - pip:
    - numba

    - sphinx==7.1.2 #Just for doc building
    - sphinx-rtd-theme==1.3.0rc1 #Just for doc building